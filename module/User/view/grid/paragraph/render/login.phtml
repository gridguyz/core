<?php

$auth   = $this->authentication();
$which  = $auth->hasIdentity() ? 'logout' : 'login';

?>
<?= $this->render( 'grid/user/authentication/' . $which, array(
    'form'      => $this->paragraph->getForm( $which ),
    'display'   => $this->paragraph->getDisplay(),
) ) ?>
